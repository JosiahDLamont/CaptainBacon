package captainbacon.gui.menu;

import captainbacon.gui.GUI;
import javafx.stage.Stage;

/**
 * Defines the GUI for the save selection (& creation?) menu.
 */
public class SaveMenuGUI extends GUI {
    public SaveMenuGUI(Stage window) {
        super(window);
    }
}
